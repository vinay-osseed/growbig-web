# Production Config Split

This split is for live/production-only configuration.

Examples:

- production caching
- production logging
- production-only integrations

Environment activation:

    DRUPAL_ENV=prod
