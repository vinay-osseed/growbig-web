# Development Config Split

This split is for local and shared development-only configuration.

Examples:

- dblog
- field_ui
- devel
- local-only logging
- verbose development settings

Environment activation:

    DRUPAL_ENV=dev
