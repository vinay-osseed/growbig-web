<?php

declare(strict_types=1);

/**
 * @file
 * Example production override.
 *
 * Copy to settings.prod.php only on the production server if extra overrides
 * are needed. Prefer environment variables for database credentials and secrets.
 * Do not commit settings.prod.php.
 */

// Example only.
// $settings['file_public_base_url'] = 'https://static.example.com/files';

// Example only.
// $settings['reverse_proxy_addresses'] = ['127.0.0.1'];
