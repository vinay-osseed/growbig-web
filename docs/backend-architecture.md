# Backend Architecture

This project uses Drupal as the main content platform and future API provider.

The backend should provide reusable content types, components, media, menus, site settings, environment-specific configuration, and optional API output for future frontend use.
