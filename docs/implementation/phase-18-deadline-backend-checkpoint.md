# Phase 18: Deadline Backend Checkpoint

## Goal

Move faster without losing safety by verifying multiple backend areas in one checkpoint script.

This checkpoint confirms:

- Webform-backed forms still work.
- YAML components remain present after the Phase 09B form correction.
- Media asset model and API work.
- Existing page/menu/route/content/setup flows still pass.

## Main Script

    ./scripts/setup/verify-deadline-backend-checkpoint.sh

## Important Notes

This is not final production hardening.

It is a safe backend checkpoint for the current deadline so we can continue with less one-by-one overhead.
